<div class="container">
  {{nbd-header}}

  <div class="page-content">
    <section class="releases">
      {{#each model as |release|}}
        <div class="row release u-cf">
          <div class="one-half column release-image-container">
            <img src={{release.image}} alt={{release.artist}}>
          </div>
          <div class="one-half column release-description">
            <p class="release-id">nbd00{{release.id}}</p>
            <p class="release-artist-album-format">{{release.artist}} "{{release.album}}" {{release.format}}</p>
            <p class="release-date">released {{moment-format release.releasedAt 'MMMM DD YYYY'}}</p>
            <p>{{release.blurbP1}}</p>
            {{#if release.blurbP2}}
              <p>{{release.blurbP2}}</p>
            {{/if}}
            {{#if release.blurbP3}}
              <p>{{release.blurbP3}}</p>
            {{/if}}
            <nav>
              {{#if release.bandcamp}}
                <a href={{release.bandcamp}} target="_blank">
                  {{fa-icon icon="play"}}
                </a>
              {{/if}}
              {{#if release.itunes}}
                <a href={{release.itunes}} target="_blank">
                  {{fa-icon icon="apple"}}
                </a>
              {{/if}}
              {{#if release.spotify}}
                <a href={{release.spotify}} target="_blank">
                  {{fa-icon icon="spotify"}}
                </a>
              {{/if}}
            </nav>
          </div>
        </div>
      {{/each}}
    </section>
  </div>
</div>
